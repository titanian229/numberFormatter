// const inquirer = require("inquirer");
const path = require("path");
const fs = require("fs");

async function mainApp() {
    console.log("Welcome to the Number Formatter, if this is your first time using it please note:\n\nThis will REPLACE the existing file.  It will also write a file with a line by line description of what was changed and the line numbers.  It is highly recommended to have committed and pushed recently in case you wish to revert these changes.")
}